import React from "react";
import { Title, WithPadding } from "../../components";

function AppsConfiguration(): React.ReactElement {
  return (
    <WithPadding>
      <Title label="Apps Configuration" />
    </WithPadding>
  );
}

export default AppsConfiguration;
